[package]
name = "fastqtofastafast"
version = "0.1.0"
edition = "2024"

[dependencies]
flate2 = { version = "1.1.2", features = ["zlib-rs"] }
zstd = { version = "0.13.3", features = ["zstdmt"] }
kdam = "0.6.3"

[profile.release]
codegen-units = 1
lto = "fat"